import { describe, expect, it } from 'vitest'; import { diffTraces } from '../src/diff.js'; import { ReplayStore } from '../src/replay.js'; import { TRACE_SCHEMA_VERSION, type AgentTrace } from '../src/core/types.js';
const trace:AgentTrace={schemaVersion:TRACE_SCHEMA_VERSION,runId:'run_1',status:'completed',startedAt:new Date().toISOString(),output:{answer:'a'},events:[{id:'e1',type:'tool.result',name:'search',timestamp:new Date().toISOString(),data:{result:'ok'}},{id:'e2',type:'llm.response',name:'llm',timestamp:new Date().toISOString(),data:{text:'answer'}}]};
describe('diff/replay',()=>{ it('detects output changes',()=>{ expect(diffTraces(trace,{...trace,output:{answer:'b'}}).differences).toContain('final output changed'); }); it('replays recorded outputs',()=>{ const s=new ReplayStore(trace); expect(s.nextToolResult('search')).toEqual({result:'ok'}); expect(s.nextLlmResponse()).toEqual({text:'answer'}); }); });
