import { mkdtemp, rm } from 'node:fs/promises'; import { tmpdir } from 'node:os'; import { join } from 'node:path'; import { describe, expect, it } from 'vitest'; import { createRecorder } from '../src/core/recorder.js';
describe('recorder',()=>{ it('records ordered completed trace',async()=>{ const dir=await mkdtemp(join(tmpdir(),'agentrec-')); try{ const r=createRecorder({runDir:dir}); r.startRun({q:'hello'}); r.recordToolCall('search',{query:'hello'}); r.recordToolResult('search',{result:'world'}); const t=await r.finishRun({answer:'world'}); expect(t.status).toBe('completed'); expect(t.events.map(e=>e.type)).toEqual(['run.started','tool.call','tool.result','run.finished']); } finally{ await rm(dir,{recursive:true,force:true}); }}); });
